import { logo } from "../assets";

const hero = () => {
  return (
    <div className="hero">
      <h1 className="hero-text">
        Summarize Articles with <br />
        <span>Helios, using GPT-4</span>
      </h1>
      <h2>
        Simplify your reading with <span>Helios</span>, an article summarizer
        that transforms length articles into clear and concise summaries.
      </h2>
    </div>
  );
};

export default hero;
